<Window x:Class="SpaSalon.Views.JournalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Журнал действий" 
        Height="500" 
        Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель фильтров -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Период:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="StartDatePicker" Width="120" Height="30"/>
                <TextBlock Text=" — " VerticalAlignment="Center" Margin="10,0"/>
                <DatePicker x:Name="EndDatePicker" Width="120" Height="30"/>
                <Button x:Name="FilterButton" Content="Фильтр" Width="80" Height="30" 
                        Margin="20,0,0,0" Background="#3498DB" Foreground="White"
                        Click="FilterButton_Click"/>
                <Button x:Name="RefreshButton" Content="Обновить" Width="80" Height="30" 
                        Margin="10,0,0,0" Background="#27AE60" Foreground="White"
                        Click="RefreshButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Таблица журнала -->
        <DataGrid x:Name="JournalDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Дата и время" Binding="{Binding TimestampString}" Width="150"/>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding UserName}" Width="150"/>
                <DataGridTextColumn Header="Действие" Binding="{Binding Action}" Width="150"/>
                <DataGridTextColumn Header="Подробности" Binding="{Binding Details}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>