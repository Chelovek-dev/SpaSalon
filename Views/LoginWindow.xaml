<Window x:Class="SpaSalon.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация - СПА Салон" 
        Height="550" 
        Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Storyboard x:Key="ButtonHover">
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                             To="1.05" Duration="0:0:0.1"/>
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                             To="1.05" Duration="0:0:0.1"/>
        </Storyboard>
        <Storyboard x:Key="ButtonLeave">
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" 
                             To="1" Duration="0:0:0.1"/>
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" 
                             To="1" Duration="0:0:0.1"/>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <!-- Фоновый градиент -->
        <Border>
            <Border.Background>
                <LinearGradientBrush EndPoint="1,1" StartPoint="0,0">
                    <GradientStop Color="#2C3E50" Offset="0"/>
                    <GradientStop Color="#1A252F" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Карточка входа -->
        <Border Width="380" Height="480" Background="White" CornerRadius="20" 
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>

            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Иконка и заголовок -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="💆‍♀️" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="СПА Салон" FontSize="28" FontWeight="Bold" 
                               Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBlock Text="Система управления" FontSize="14" Foreground="#7F8C8D" 
                               HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Поле телефона -->
                <TextBlock Grid.Row="1" Text="Телефон" FontSize="13" FontWeight="SemiBold" 
                           Foreground="#555" Margin="0,0,0,5"/>
                <TextBox x:Name="LoginTextBox" Grid.Row="2" Height="45" FontSize="14"
                         Margin="0,0,0,20"/>

                <!-- Поле пароля -->
                <TextBlock Grid.Row="3" Text="Пароль" FontSize="13" FontWeight="SemiBold" 
                           Foreground="#555" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="4" Height="45" FontSize="14"
                             Margin="0,0,0,25"/>

                <!-- Кнопка входа -->
                <Button x:Name="LoginButton" Grid.Row="5" Content="ВОЙТИ" 
                        Height="50" FontSize="16" FontWeight="Bold"
                        Background="#3498DB" Foreground="White" BorderThickness="0"
                        Cursor="Hand" Click="LoginButton_Click"
                        RenderTransformOrigin="0.5,0.5">
                    <Button.RenderTransform>
                        <ScaleTransform/>
                    </Button.RenderTransform>
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.MouseEnter">
                            <BeginStoryboard Storyboard="{StaticResource ButtonHover}"/>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Button.MouseLeave">
                            <BeginStoryboard Storyboard="{StaticResource ButtonLeave}"/>
                        </EventTrigger>
                    </Button.Triggers>
                </Button>

                <!-- Сообщение об ошибке -->
                <TextBlock x:Name="ErrorTextBlock" Grid.Row="6" Text="" Foreground="#E74C3C" 
                           FontSize="12" HorizontalAlignment="Center" Margin="0,15,0,0"
                           Visibility="Visible"/>
            </Grid>
        </Border>
    </Grid>
</Window>